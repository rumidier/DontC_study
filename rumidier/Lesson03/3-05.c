#include <stdio.h>

int
main (int   argc,
      char *argv[])
{
  printf ("트라이그래프 ??'은 단일문자 ^으로 대체 된다. \n");
  printf ("트라이그래프 ??\은 단일문자 ^으로 대체 된다. \n");
  printf ("트라이그래프 \?\?'은 단일문자 ^으로 대체 된다. \n");

  return 0;
}
